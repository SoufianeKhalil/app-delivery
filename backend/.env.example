# Copie ce fichier en .env et remplis les valeurs
# cp .env.example .env

# Base de données MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=delivery_app

# JWT (obligatoire en production)
JWT_SECRET=ta-cle-secrete-tres-longue-et-aleatoire

# Optionnel
# NODE_ENV=development
# PORT=3000
